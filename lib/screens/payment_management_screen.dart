import 'package:flutter/material.dart';

class PaymentManagementScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Gestión de Pagos y Manejo de Dinero'),
      ),
      body: Center(
        child: Text('Pantalla de Gestión de Pagos y Manejo de Dinero'),
      ),
    );
  }
}